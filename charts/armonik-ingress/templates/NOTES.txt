***********************************************************************
* AdminGUI                                                            *
***********************************************************************
  Chart version: {{ .Chart.Version }}
  App version:   {{ .Chart.AppVersion }}
  Get the application URL by running these commands:
    kubectl port-forward -n armonik service/admin-gui 1080:1080
***********************************************************************
{{- if list .Values "networkPolicy" "enabled" | include "armonik.utils.index" }}
  NetworkPolicy is enabled for the ingress and GUI pods. This chart's own rule allows DNS and
  the GUI backend only. Add an egress rule for each backend you connect to under
  networkPolicy.nginx.extraEgressRules.
{{- end }}
