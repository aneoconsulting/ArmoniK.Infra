suite: shared issuer namespace guard
release:
  name: t
templates:
  - certmanager-issuer-guard.yaml
tests:
  - it: passes when every consumer's namespace matches the shared issuer's namespace
    set:
      global:
        armonik:
          certManager:
            issuer:
              enabled: true
              name: shared-issuer
      certManagerIssuer:
        create: true
        provider: venafi
        venafi:
          zone: my-zone
      ingress:
        tls:
          enabled: true
          certManager:
            enabled: true
      dependencies:
        activemq:
          tls:
            enabled: true
            type: cert-manager
        mongodb:
          certManager:
            enabled: true
        redis:
          tls:
            enabled: true
            certManager:
              enabled: true
    asserts:
      - notFailedTemplate: {}

  - it: fails when ingress renders its Certificate into a different namespace than the shared issuer
    set:
      global:
        armonik:
          certManager:
            issuer:
              enabled: true
              name: shared-issuer
      certManagerIssuer:
        create: true
        provider: venafi
        venafi:
          zone: my-zone
      ingress:
        namespaceOverride: other-ns
        tls:
          enabled: true
          certManager:
            enabled: true
    asserts:
      - failedTemplate:
          errorPattern: 'ingress renders its Certificate into namespace "other-ns"'

  - it: fails when the umbrella's namespaceOverride moves the shared issuer away from redis, which cannot follow
    set:
      namespaceOverride: other-ns
      control-plane:
        namespaceOverride: other-ns
      compute-plane:
        namespaceOverride: other-ns
      global:
        armonik:
          certManager:
            issuer:
              enabled: true
              name: shared-issuer
      certManagerIssuer:
        create: true
        provider: venafi
        venafi:
          zone: my-zone
      dependencies:
        redis:
          tls:
            enabled: true
            certManager:
              enabled: true
    asserts:
      - failedTemplate:
          errorPattern: 'dependencies.redis renders its Certificate into namespace "NAMESPACE" but the shared issuer lives in "other-ns"'

  - it: fails when dependencies.activemq renders its Certificate into a different namespace than the shared issuer
    set:
      global:
        armonik:
          certManager:
            issuer:
              enabled: true
              name: shared-issuer
      certManagerIssuer:
        create: true
        provider: venafi
        venafi:
          zone: my-zone
      dependencies:
        activemq:
          namespaceOverride: other-ns
          tls:
            enabled: true
            type: cert-manager
    asserts:
      - failedTemplate:
          errorPattern: 'dependencies.activemq renders its Certificate into namespace "other-ns"'

  - it: fails when dependencies.mongodb renders its Certificates into a different namespace than the shared issuer
    set:
      global:
        armonik:
          certManager:
            issuer:
              enabled: true
              name: shared-issuer
      certManagerIssuer:
        create: true
        provider: venafi
        venafi:
          zone: my-zone
      dependencies:
        mongodb:
          namespaceOverride: other-ns
          certManager:
            enabled: true
    asserts:
      - failedTemplate:
          errorPattern: 'dependencies.mongodb renders its Certificates into namespace "other-ns"'

  - it: does not fire for a cluster-scoped issuer kind even when every consumer's namespace diverges
    set:
      global:
        armonik:
          certManager:
            issuer:
              enabled: true
              name: shared-issuer
              kind: ClusterIssuer
      certManagerIssuer:
        create: true
        provider: venafi
        venafi:
          zone: my-zone
      ingress:
        namespaceOverride: other-ns-1
        tls:
          enabled: true
          certManager:
            enabled: true
      dependencies:
        activemq:
          namespaceOverride: other-ns-2
          tls:
            enabled: true
            type: cert-manager
        mongodb:
          namespaceOverride: other-ns-3
          certManager:
            enabled: true
    asserts:
      - notFailedTemplate: {}

  - it: does not fire for a consumer with certManager.provider set even when its namespace diverges from the shared issuer
    set:
      global:
        armonik:
          certManager:
            issuer:
              enabled: true
              name: shared-issuer
      certManagerIssuer:
        create: true
        provider: venafi
        venafi:
          zone: my-zone
      ingress:
        namespaceOverride: other-ns-1
        tls:
          enabled: true
          certManager:
            enabled: true
            provider: selfSigned
      dependencies:
        activemq:
          namespaceOverride: other-ns-2
          tls:
            enabled: true
            type: cert-manager
            certManager:
              provider: selfSigned
        mongodb:
          namespaceOverride: other-ns-3
          certManager:
            enabled: true
            provider: ca
            ca:
              secretName: mongo-ca
    asserts:
      - notFailedTemplate: {}

  - it: still fires for a consumer without certManager.provider set even when a sibling consumer sets it
    set:
      global:
        armonik:
          certManager:
            issuer:
              enabled: true
              name: shared-issuer
      certManagerIssuer:
        create: true
        provider: venafi
        venafi:
          zone: my-zone
      dependencies:
        activemq:
          namespaceOverride: other-ns
          tls:
            enabled: true
            type: cert-manager
            certManager:
              provider: selfSigned
        mongodb:
          namespaceOverride: other-ns-2
          certManager:
            enabled: true
    asserts:
      - failedTemplate:
          errorPattern: 'dependencies.mongodb renders its Certificates into namespace "other-ns-2"'
