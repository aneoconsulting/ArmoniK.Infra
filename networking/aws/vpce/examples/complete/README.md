## Complete VPC endpoints for EKS

Terraform scripts to create AWS VPC endpoints for an AWS EKS using the [module vpce](../..).

To create AWS VPC endpoints for EKS:

```bash
terraform init
terraform plan
terraform apply
```

To delete all resource:

```bash
terraform destroy
```



