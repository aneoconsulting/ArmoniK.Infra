***********************************************************************
* Compute Plane                                                       *
***********************************************************************
  Chart version: {{ .Chart.Version }}
  App version:   {{ .Chart.AppVersion }}
***********************************************************************
{{- if list .Values "networkPolicy" "enabled" | include "armonik.utils.index" }}
  NetworkPolicy is enabled for this compute-plane release.
  When deployed by the armonik umbrella, also set the umbrella's top-level
  networkPolicy.enabled=true. Enabling the policy in this chart alone does not
  enable the umbrella-owned cross-component egress rules.
{{- end }}
