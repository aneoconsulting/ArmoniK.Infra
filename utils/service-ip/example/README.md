# Kubernetes service

To create a Kubernetes service and get its domain, IP, and port:

```bash
terraform init
terraform plan
terraform apply
```

To delete all resource:

```bash
terraform destroy
```
