# Expected failure: podSpecPatch may not replace a list the chart builds itself.
partitions:
  default: {}
partitionCommon:
  worker:
    image:
      repository: dockerhubaneo/armonik-dynamic-dotnet-worker
  podSpecPatch:
    containers:
      - name: hijack
        image: busybox:0.0.0-test
