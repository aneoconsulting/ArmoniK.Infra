apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "compute-plane.fullname" . }}-test-connection"
  labels:
    {{- include "compute-plane.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: 
      command: ['wget']
      args: ['{{ include "compute-plane.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
