## Complete VPC

Terraform scripts to create an AWS VPC for an AWS EKS using the [module vpc](../..).

To create an AWS VPC for an AWS EKS:

```bash
terraform init
terraform plan
terraform apply
```

To delete all resource:

```bash
terraform destroy
```



